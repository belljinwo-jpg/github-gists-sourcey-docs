# Maintainer inspection report

- Start with the live documentation at https://belljinwo-jpg.github.io/ and confirm that the root redirects to the generated Gists page, both navigation tabs load, and search exposes endpoint and schema entries.
- The source of truth is GitHub's maintained `github/rest-api-description` repository at commit `1847198856aa730d19ac7fef6f2617ecbec8e01e`; the selected YAML blob is `26ab99a5c0ffe590185a3dfed7c96bf5871a7996` and the repository license is MIT.
- Reproduce the exact output with `pnpm install --frozen-lockfile && pnpm run build`. Sourcey 3.6.3 reads the pinned OpenAPI document through `sourcey.config.ts`, while `scripts/split-openapi.mjs` includes exactly twenty Gists operations and recursively retains their referenced components.
- Inspect `gists.html` and `gist-management.html` first. Each contains ten real REST operations, giving coherent coverage of listing, creating, retrieving, updating, deleting, starring, un-starring, forking, and revision access rather than a collection of placeholder pages.
- Inspect `evidence.json` next. It records the repo, commit, blob, MIT license, adapter, build command, generated page list, SHA-256 hashes, forty-one search entries, coverage notes, known gaps, and Sourcey friction in nine observations.
- The governed validation used `runx-cli 0.6.8`. Its sealed receipt is `sha256:af5b4f7a13a122ee437b529ff83d7e8e3258f1a9183026dc143193114f695fbc` at `receipts/sourcey-validation.json`; `receipts/verification.json` records a valid content address, digest, and production-mode Ed25519 signature.
- The public CI run at https://github.com/belljinwo-jpg/github-gists-sourcey-docs/actions/runs/27876502192 installs the pinned runx version, fetches the stranger-visible site and evidence, checks all seven acceptance assertions, seals the receipt, verifies its signature, and commits the proof artifacts.
- Known scope gap: this site deliberately documents the coherent Gists surface, not every GitHub REST operation. Sourcey preserves upstream descriptions, schemas, examples, and source identity, but does not emit line-number deep links into the upstream YAML.
- Known Sourcey friction: version 3.6.3 omitted `consola` from its runtime dependencies, and large single-page output needed deterministic splitting and minification for reliable static delivery. Both workarounds are explicit and reproducible in the repository.
